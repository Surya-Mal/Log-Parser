You will need to use the authentication logs in your own system (This one is develoup for Ubuntu)
- Ex: /var/log/auth.log

Will later work to make a more consistant log parser to work on different OS's
